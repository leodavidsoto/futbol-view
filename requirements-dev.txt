# Football Copilot — dependencias para desarrollo y CI
#
# Deliberadamente ligeras: la suite de tests no necesita YOLO, SAHI, Norfair ni
# PyTorch. El paquete `fcopilot` importa esas dependencias de forma opcional y
# los tests inyectan un detector guionizado, así que CI instala ~80 MB en vez de
# varios gigabytes y termina en menos de un minuto.
#
# Para ejecutar el sistema de verdad:  pip install -r requirements_v2.txt

numpy>=1.26,<3
opencv-python-headless>=4.10          # features de color de camiseta y E/S de vídeo
scikit-learn>=1.5                     # KMeans de clasificación de equipos

fastapi>=0.112
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pillow>=10.4

pytest>=8.0
pytest-cov>=5.0
pytest-timeout>=2.3                   # ningún test debe colgar CI
httpx>=0.27                           # cliente de fastapi.testclient
